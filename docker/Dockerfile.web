# Використовуємо базовий образ Apache HTTP Server
FROM httpd:2.4

# Копіюємо файли додатку до директорії, яку використовує Apache для веб-сторінок
COPY . /usr/local/apache2/htdocs/

# Відкриваємо порт 80, якщо потрібно доступ до сервера зовні
EXPOSE 80
