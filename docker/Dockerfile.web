# Використовуємо базовий образ, створений Dockerfile.base
FROM node-app-base

# Копіюємо решту файлів додатку
COPY . .

# Відкриваємо порт, на якому буде працювати додаток
EXPOSE 3000

# Запускаємо додаток
CMD ["npm", "start"]